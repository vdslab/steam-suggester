export type steamGameCategoryType = {
  id: number,
  description: string
}

export type steamGameGenreType = {
  id: string,
  description: string
}

export type steamGameDeviceType = {
  windows: boolean,
  mac: boolean,
}